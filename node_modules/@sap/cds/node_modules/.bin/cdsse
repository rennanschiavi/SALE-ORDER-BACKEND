#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../../../cds-compiler/bin/cdsse.js" "$@"
  ret=$?
else 
  node  "$basedir/../../../cds-compiler/bin/cdsse.js" "$@"
  ret=$?
fi
exit $ret
